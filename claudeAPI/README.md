# claudeAPI

An example of sending a query and getting a response from the [Claude API](https://platform.claude.com/dashboard).

**Note:** You will need an API key and an account for this to work; you must also make sure to NOT store your API key in any publicly accessible place (like GitHub).
